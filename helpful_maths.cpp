#include<iostream>
using namespace std;

int main(){
    int s,i,j;
    cin>>s;
    int p[(s.size()/2)+1];
    string n;
    for(i=0;i<s.size();i++){
        for(j=0;j<s.size();j+=2)
    }
    int count=0;
    cout<<count;
}
